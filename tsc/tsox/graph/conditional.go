package graph

// ExpressionConditional evaluates Operand once, then only Left (true) or Right
// (false). Its result is the selected arm value, in the declared common domain.
const ExpressionConditional ExpressionKind = "conditional"
